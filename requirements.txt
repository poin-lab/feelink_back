# requirements.txt

# --- 웹 프레임워크 및 서버 ---
# 핵심 웹 프레임워크
fastapi

# 개발용 ASGI 서버 (표준 추가 기능 포함)
uvicorn[standard]

# 프로덕션(배포) 환경용 WSGI/ASGI 서버
gunicorn

# HTTP 클라이언트
httpx

# 이미지 처리
PIL

# --- 데이터베이스 (ORM & 드라이버) ---
# Python 객체와 DB 테이블을 매핑해주는 비동기 지원 ORM (Object-Relational Mapper)
SQLAlchemy

# SQLAlchemy가 PostgreSQL과 비동기 통신을 하기 위해 사용하는 저수준 드라이버
asyncpg


# --- AI 서비스 ---
# Google Gemini API와 통신하기 위한 공식 라이브러리
google-generativeai


# --- 클라우드 및 유틸리티 ---
# 업로드된 이미지를 Azure Blob Storage에 저장하기 위한 라이브러리
azure-storage-blob

# FastAPI에서 이미지 등 파일 업로드(multipart/form-data)를 처리하기 위해 필수
python-multipart

# 로컬 개발 시 .env 파일에서 환경변수를 불러오기 위한 라이브러리
python-dotenv